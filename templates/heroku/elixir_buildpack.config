# Erlang version
erlang_version=19.3

# Elixir version
elixir_version=<%= @elixir_version %>

# Always rebuild from scratch on every deploy?
always_rebuild=false

# Release Elixir App
post_compile="mix release --verbose"
